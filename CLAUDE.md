# Alpha-Trade Engine

## Secrets

Always load API keys and other secrets from `.env` (see `README.md` → "Environment variables").
Never hardcode a key/token/secret directly in code, and never print or log one, so a key doesn't
end up exposed in the code or in a screenshot/output.
